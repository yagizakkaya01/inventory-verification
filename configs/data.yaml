# Ultralytics dataset config (YOLO format). Built by scripts/build_dataset.py
# from the Label Studio annotations. Paths resolve against `path`.

path: data/datasets/inventory
train: images/train
val: images/val
test: images/test

names:
  0: tufek
  1: tabanca
  2: sarjor
