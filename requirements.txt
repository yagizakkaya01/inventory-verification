# Core — install PyTorch SEPARATELY first, matched to your CUDA version:
#   https://pytorch.org/get-started/locally/
# this machine (RTX 4070 Laptop, driver CUDA 13.3):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#
# Then:  pip install -r requirements.txt
# (exact resolved versions: requirements-lock.txt)

ultralytics>=8.3.0          # YOLO training + inference
opencv-python>=4.9.0
numpy>=1.26
pyyaml>=6.0
pydantic>=2.6               # typed config models

# --- Camera SDKs ---
depthai>=3.9                # OAK-D (v3 API — different from v2!)
# openni                    # Orbbec Astra (or vendor SDK) — add if Astra is used
# pyorbbecsdk               # Orbbec (newer SDK)

# --- Edge export ---
blobconverter>=1.4          # OAK-D .blob conversion (ONNX -> MyriadX)
# onnx>=1.16
# onnxruntime-gpu>=1.17
# TensorRT: comes with JetPack on Jetson; on desktop install via NVIDIA

# --- Dev ---
pytest>=8.0
